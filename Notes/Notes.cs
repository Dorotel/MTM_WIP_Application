/*
 *Control_AdvancedRemove_Button_Search_Click
 * if like textbox has text in it then
 *  if like combobox = partid then "SELECT * FROM Parts WHERE PartID LIKE '%textbox.Text%'"
 *  if like combobox = location then "SELECT * FROM Parts WHERE Location LIKE '%textbox.Text%'"
 *  if like combobox = user then "SELECT * FROM Parts WHERE User LIKE '%textbox.Text%'"
 *
 *  but use the textbox.text as a parameter
 *  table is inv_inventory
 *
 *
 */

